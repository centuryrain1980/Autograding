# AutoGrade Pro (智能作业批改助手)

AutoGrade Pro 是一款专为教师和助教设计的现代化智能作业批改工具。它结合了先进的 AI 模型（如 Google Gemini 2.5, DeepSeek, 通义千问等）与 OCR 技术，能够自动识别作业内容、根据自定义标准进行评分，并生成详细的批改报告。

## ✨ 核心功能

### 1. 多模态作业支持
*   **文档支持**: 完美支持 Word (`.docx`) 和 PDF 文档，自动提取文本内容。
*   **图片支持**: 支持 JPG/PNG 格式的作业图片（需使用支持视觉能力的 AI 模型，如 Gemini Flash 或 Qwen-VL）。
*   **批量处理**: 支持拖拽上传多个文件或直接导入整个文件夹。

### 2. 灵活的评分标准 (Rubric)
*   **自定义维度**: 用户可以自由编写评分标准（Prompt），AI 将严格遵循。
*   **多样化评分**:
    *   **定量评分**: 支持百分制（如 85/100）。
    *   **定性评价**: 支持等级制（如 A/B/C、优/良/中/差）。
    *   **分项打分**: 支持按章节或知识点单独打分（如“第一章：10分”，“逻辑性：A”）。

### 3. 智能批改与反馈
*   **详细反馈**: 自动生成总评、各分项得分与评语、优点、不足及改进建议。
*   **结构化数据**: 批改结果包含详细的 JSON 结构，便于后续数据处理。
*   **可视化仪表盘**: 实时展示批改进度、分数分布统计图表。

### 4. 结果管理与导出
*   **详细视图**: 点击任意作业可查看完整的批改详情卡片。
*   **动态表格**: 结果列表会根据评分标准中的分项自动生成对应列（如自动把“语法”、“逻辑”拆分为独立列）。
*   **Excel/CSV 导出**: 支持一键导出所有批改结果。
    *   **智能格式化**: 导出时自动清洗分数格式（去除分母），便于 Excel 直接计算。
    *   **分列显示**: 所有分项的得分和评语均独立分列，方便统计分析。

## 🛠️ 使用指南

### 第一步：导入作业 (Import)
1. 进入 **“导入作业”** 页面。
2. 点击“选择文件”或“导入文件夹”。
3. 支持格式：`.docx`, `.pdf`, `.jpg`, `.png`。
   > **注意**: 不支持旧版 `.doc` 格式，请先将其另存为 `.docx`。

### 第二步：设定标准 (Rubric)
1. 进入 **“评分标准”** 页面。
2. 在编辑框中输入您的评分细则。例如：
   ```text
   1. 准确性 (40分): 答案是否正确？
   2. 逻辑过程 (30分): 推理步骤是否清晰？
   3. 卷面分 (30分): 字迹是否工整？
   ```
   或者定性标准：
   ```text
   - 概念理解 (优/良/差)
   - 创新思维 (A/B/C)
   ```

### 第三步：批量批改 (Batch Grade)
1. 回到 **“仪表盘”** (Dashboard)。
2. 点击 **“开始批量批改”** 按钮。
3. 系统将逐个处理队列中的文件，状态会从“待处理”变为“处理中”最终变为“已完成”。

### 第四步：查看与导出 (Results)
1. 进入 **“批改结果”** 页面。
2. 查看详细的成绩单。
3. 点击 **“导出 Excel (CSV)”** 下载分析表格。

## ⚙️ 系统设置与模型配置

AutoGrade Pro 支持多种 AI 模型后端，可在 **“系统设置”** 页面进行切换：

### 1. Google Gemini (推荐)
*   **特点**: 速度快、多模态能力强（原生支持图片）、长文本处理能力优秀。
*   **模型**: Gemini 2.5 Flash (默认), Gemini 2.5 Pro。
*   **配置**: 默认使用环境变量中预置的 `API_KEY`。

### 2. Custom / OpenAI Compatible
支持所有兼容 OpenAI 接口格式的模型服务商（如 SiliconFlow, 阿里云百炼等）。
*   **支持模型**:
    *   **DeepSeek V3**: 性价比极高的文本推理模型。
    *   **Qwen Plus (通义千问)**: 强大的中文文本模型。
    *   **Qwen VL / Qwen2.5 VL**: 具备视觉能力的国产模型，支持图片批改。
*   **配置**: 需在设置页面手动输入您的 `API Key` 和 `Base URL` (例如 `https://api.siliconflow.cn/v1`)。

## ⚠️ 常见问题

*   **Q: 为什么上传 `.doc` 文件报错？**
    *   A: 由于浏览器端技术限制，目前仅支持标准的 Office Open XML 格式 (`.docx`)。请在 Word 中“另存为” .docx 格式后再上传。
*   **Q: 图片批改需要注意什么？**
    *   A: 请确保在设置中选择了支持视觉 (Vision) 的模型（如 Gemini Flash, Qwen-VL）。如果使用纯文本模型（如 DeepSeek V3）批改图片，将会报错。
*   **Q: 导出 CSV 后乱码怎么办？**
    *   A: 导出的 CSV 文件已包含 BOM 头，通常 Excel 可以直接正常打开。如果出现乱码，请尝试使用“数据 -> 导入文本”功能，并选择 UTF-8 编码。

## 📦 部署与安装 (Deployment)

本项目基于 React + Vite 构建，可以轻松部署到任何静态托管平台。

### 本地运行
1. **环境准备**: 确保已安装 Node.js 18+。
2. **安装依赖**:
   ```bash
   npm install
   ```
3. **启动开发服务器**:
   ```bash
   npm run dev
   ```

### 部署到 Vercel (推荐)
1. 将代码推送到 GitHub 仓库。
2. 登录 [Vercel](https://vercel.com) 并导入该仓库。
3. (可选) 在 Settings -> Environment Variables 中配置 `VITE_API_KEY`（如果不配置，用户需在界面手动输入 Key）。
4. 点击 **Deploy**，即可获得分享链接。

### 构建静态文件
如需部署到 Nginx 或其他服务器：
```bash
npm run build
```
构建产物将位于 `dist` 目录中。

## 技术栈
*   **前端**: React, Tailwind CSS
*   **构建工具**: Vite
*   **AI SDK**: Google GenAI SDK
*   **文档解析**: Mammoth.js (.docx), PDF.js (.pdf)
*   **图标库**: Lucide React

---
*v1.1.0 Windows Edition*